{
  "name": "voice-kb",
  "version": "0.3.0",
  "private": true,
  "description": "Voice agent that answers questions on one person's behalf, backed by Postgres.",
  "main": "server.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "migrate": "node db/migrate.js",
    "seed": "node db/seed.js",
    "setup": "npm run migrate && npm run seed",
    "gaps": "node db/gaps.js"
  },
  "dependencies": {
    "pg": "^8.13.1"
  }
}
